# 模型训练协议

原则：Experiment 管研究路线、代码、配置和环境；run 管一次完整训练及其输出；Git 管版本，W&B 管记录；交付包含有效训练、评估和代码清理。

## Experiment 与 run

- **Experiment** 是一条研究路线，使用稳定的 `experiment_id`。核心建模方法、模型架构或训练算法的重大变化才新建 Experiment；调参、工程修复和数据选择调整在原 Experiment 内完成。
- **run** 是一次独立训练试验，使用唯一的 `run_id`。输入校验、预处理、训练和评估是同一 run 的连续步骤，共用输出目录和 W&B run，不为阶段单独创建 run。
- 阶段切换、同配置的校验修复、临时故障重试和断点续训沿用原 run，追加日志并保留已有记录。恢复训练须恢复 checkpoint 中的训练状态，不能只恢复 W&B 日志或模型权重。
- 从头重训或改变数据、模型、训练配置等会影响结果的条件时，新建 run。修复会使已有训练结果失效的代码也应重新训练；不得将不同条件的训练历史混在同一 run。
- 历史结果及其评估口径保持不变。比较 runs 时说明数据、划分、指标等差异；独立复现创建新 run，并记录来源。

## 目录职责

```text
data/                         # 已登记的共享数据，训练期间只读
models/                       # 固定版本的第三方模型及权重
scripts/                      # 有持续用途的通用数据处理与维护工具
docs/                         # 数据说明、调查结论和跨实验文档
experiments/<experiment_id>/
  PLAN.md                     # 研究问题、方法、基线和评估方案
  REPORT.md                   # run 结果比较、结论与局限
  src/                        # 正式训练、评估流程所需的模块
  tests/                      # 必要的功能验证
  configs/                    # 可复用的训练配置
  pyproject.toml
  uv.lock
  .venv/                      # 本 Experiment 共用的环境
  reproduce.sh                # 完整训练入口，支持恢复
  outputs/<run_id>/
    config.yaml               # 实际配置及数据、代码、环境版本引用
    train.log
    metrics.json
    checkpoints/
    predictions/
    cache/                    # 本次预处理结果及临时文件
    wandb/
```

目录和产物按需创建。Experiment 根目录维护代码、配置、依赖和研究文档；本次执行产生的文件统一进入对应 run。禁止在根目录扩展 `preflight/`、`preparation/` 等阶段目录，或建立另一套阶段管理流程。run 不另建源码项目、虚拟环境、PLAN、REPORT 或 manifest。

## 代码保留与主动清理

- **先复用，再新增**：修改前查找已有实现，优先更新所属模块。新文件应承担当前需要的独立职责；调参用配置，修复更新原实现，避免积累替代版本。按职责组织模块，不按操作次数或调查步骤拆文件。
- **保留当前需要的代码**：`src/` 中的模块须服务于正式训练或评估流程，包括其数据处理与校验。脚本之间相互引用不构成保留理由；应追溯到实际功能。`scripts/` 同样要求明确的持续用途，不能用来转存一次性脚本。
- **临时代码临时执行**：一次性查询、排障和探索使用命令行或系统临时目录，用后清理。需要持续复现的数据转换整理为正式模块；调查结论记入文档。
- **主动删除**：每次交付和提交前，检查本次新增、修改及被替代的代码，删除临时脚本、重复实现、废弃分支，以及随之失效的配置和测试；同步更新引用与文档。清理范围同时包含本次产生的临时代码和本次修改导致失效的旧实现。历史实现由 Git 保存。
- **验证清理结果**：保留内容必须对应当前功能、必要测试或明确的复现需求；“以后可能有用”不是保留理由。完成与改动影响相匹配的验证后再交付。其他任务的文件、已登记数据和历史 run 产物不属于此次清理范围。

## 环境与版本

- 每个 Experiment 使用一套 uv 管理的 `.venv/`，各 run 共用；不同 Experiment 不共享虚拟环境。基础 Python、uv 和原生库使用已有 micromamba 环境 `virtual-cell`，保持其版本稳定，不删除或重建。
- uv 经 `micromamba run -n virtual-cell` 调用，显式绑定该环境的 Python，并使用 `--no-python-downloads`。不回退到系统 Python，不启用系统 site-packages，不把 `UV_PROJECT_ENVIRONMENT` 指向基础环境。
- Agent 自行通过 `uv add/remove/lock` 声明和锁定实验依赖，执行时使用 `uv sync --locked`、`uv run --locked`；不绕过声明安装依赖，不在执行时自动改锁。有活动 run 时，不修改其正在使用的代码、输入或环境。
- 代码、配置模板、`pyproject.toml`、`uv.lock` 和研究文档由 Git 管理，按可独立理解的逻辑变更主动提交。正式训练使用已提交的代码和锁文件，自动记录 commit；无需逐 run 复制源码或依赖项目。历史复现从对应 Git 版本与数据版本重建运行条件，不替换正在使用的环境。

## 完整训练流程

1. **明确方案**：在 Experiment 的 `PLAN.md` 中确定研究问题、数据与划分、基线、训练方案、评估指标，以及训练结束条件（如既定轮数或正常早停）。run 的具体差异由配置表达。
2. **统一入口**：`cd experiments/<experiment_id> && ./reproduce.sh` 完成环境检查、输入校验、预处理、训练和评估；支持选择配置及按 `run_id` 恢复，不要求手工激活环境。
3. **自动记录**：在 run 的 `config.yaml` 中保存实际生效配置、种子、数据及划分的固定版本引用、Git commit、依赖锁版本、关键运行时版本和来源 run；与 W&B 使用一致的身份和配置。
4. **输入与输出归位**：共享数据只读；预处理、切分、特征和缓存写入当前 run。复用其他 run 的产物时引用明确版本并记录来源，不覆写被引用产物。输入缺失或版本不符时修复来源，不静默替换数据或放宽评估标准。
5. **评估与结论**：保存模型、预测和指标，按确定的划分评估并与基线比较，将状态、结论、局限和 W&B 链接汇总到 Experiment 的 `REPORT.md`。测试反馈用于后续选择时，明确记录其开发用途。

训练任务完成要求：按既定方案完成有效训练与评估，保存可追溯的结果，更新结论，并完成上述代码清理。效果低于基线也是有效结果。预检通过、环境就绪、仅有 smoke test 或耗尽 Agent 自设预算均不算完成；确实无法继续时如实记录失败与阻塞原因，不宣称完成。

## W&B 与产物

- 统一使用 `entity="yjcyxky"`、`project="virtual-cell-challenge"`、`group=experiment_id`、`id=run_id`。本地 run 与 W&B run 一一对应，名称包含 `run_id`；阶段通过指标前缀或字段区分。
- 记录配置、版本、训练/验证/测试指标、step、运行状态和资源使用。W&B 本地文件写入当前 run 的 `wandb/`；断网时本地记录，恢复后同步并说明同步状态。
- 保存用于评估的模型；支持断点训练时保存最近的完整训练状态。关键模型和结果上传为版本化 Artifacts，复用时引用固定版本；普通缓存无需逐项归档。
- `.venv/`、缓存、大型训练产物不进入 Git；密钥、凭据和未经授权的原始数据不上传。
